import { OpacityEffectDirective } from './opacity-effect.directive'

describe('OpacityEffectDirective', () => {
    it('should create an instance', () => {
        const directive = new OpacityEffectDirective()
        expect(directive).toBeTruthy()
    })
})
